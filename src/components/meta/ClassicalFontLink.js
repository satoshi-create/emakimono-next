/**
 * 詞書オーバーレイ / 古典モノグラム用。
 * Hina Mincho 本体は _document.js の Google Fonts で読み込む（next/head 禁止回避）。
 * 呼び出し側の互換のためコンポーネントは残す。
 */
const ClassicalFontLink = () => null;

export default ClassicalFontLink;
